VITE_OPENROUTER_KEY=sk-or-v1-your-key-here

# Optional: Semantic Scholar API key (raises rate limit from shared pool to 10 req/sec).
# Apply at https://www.semanticscholar.org/product/api#api-key-form
# VITE_SEMANTIC_SCHOLAR_KEY=your-semantic-scholar-key
